---
- name: Preparing base folders
  file:
    name: "{{ item }}"
    state: directory
    owner: root
    group: root
    mode: '0755'
  become: true
  with_items: 
    - "{{ base_install_folder }}"
    - "{{ base_data_folder }}"
